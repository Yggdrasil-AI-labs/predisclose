"""leakguard - detect internal identifiers, secrets, and PII before they leak
into public artifacts. Generic engine; org-specific patterns load from a private
config you keep out of version control."""

__version__ = "0.5.0"
